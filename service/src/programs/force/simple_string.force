x = "HELLO";
y = " WORLD\n";

OUTS(x);
OUTS(y);

z = x | y;

if (z != 0)
{
  OUTS_LITERAL("This will be a shortcut to help enable output of long string literals.\n");
  OUTS_LITERAL("This syntax will only be available for constant strings in an OUTS function, which is eh\n");
}